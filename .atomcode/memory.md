- uni-halo 前端约定：页面/组件内部 import 优先用 @ 别名（@/api、@/types、@/components/...），不用相对路径；@ 指向 ui/src。
- uni-halo 插件身份：`plugin.yaml` 的 `metadata.name` 为 **`uni-halo`**（2026-09-14 由 `uni-halo-plugin` 改名）。Halo 按 metadata.name 派生插件 ID / Console URL / `plugins/{name}/available` 检查 / 静态资源前缀 `/plugins/{name}/assets/**`，勿用旧名拼这些 URL。**刻意保留旧名**的两处：`spec.settingName: uni-halo-plugin-settings`（与 `extensions/setting.yaml` 的 metadata.name 强绑定，改名会导致插件启动失败）与 `spec.configMapName: uni-halo-plugin-configmap`（改名会丢已存配置）。自选 API 路径段已统一为 `plugins/uni-halo`（Constants.END_POINT_API_BASE_PATH 为唯一源头，role-template-*.yaml 的 `resources` 必须同步）。
- uni-halo CI/CD packages 构建方案（**已实施**）：`settings.gradle` 已 `include ':packages:float-mini-profile'`，`packages/float-mini-profile/build.gradle` 复刻 ui 的 pnpm 构建（node-gradle + pnpmBuild），根 `build.gradle` 的 `classes` 已 `dependsOn` 该子工程 `assemble`，vite 的 `copyToStatic` 插件自动拷产物到 `src/main/resources/static/floating-window/`。官方 reusable-workflows 无多 pnpm 工程支持（build-args/ui-path 均帮不上），不推荐放弃官方 workflow 自写 CI。lockfile 缓存仅覆盖 ui-path 一份，可忽略。
