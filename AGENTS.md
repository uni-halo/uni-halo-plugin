# AGENTS.md

uni-halo-plugin：Halo 2.26 插件（Java 21 + Gradle 9.4 wrapper），前端为独立 Gradle 子工程 `ui`（Vue 3.5 + Vite 8 + TypeScript + pnpm 10）。已从 1.x 仓库迁入完整业务（应用管理 / 版本管理 / 二维码 / 配置接口 / 恋爱管理 / 通知公告（含公告类型）），API 分组域名采用 `unihalo.ialley.cn`（`BASIC_DOMAIN_NAME` 为唯一源头，GVK 已切换，历史数据需按 `.docs/api-domain-change-plan-a.md` 第 5 节迁移）。

## 常用命令

- 完整构建（含 UI）：`./gradlew build` —— 根工程 `classes` 依赖 `processUiResources`，会自动触发 `:ui:assemble`（pnpm build）并把 `ui/build/dist` 拷入 `build/resources/main/ui`，因此**构建必须联网装 pnpm 依赖**（Node ^20.19 或 >=22.12，pnpm 10，CI 用 Node 24）。
- 启动内置 Halo 调试环境：`./gradlew haloServer`（来自 `run.halo.plugin.devtools` 插件）。
- 后端测试：`./gradlew test`（JUnit 5 + Mockito，见 `src/test/java`）。
- **pnpm workspace（2026-09-22 起）**：`ui` 与 `packages/float-profile-widget` 已并入仓库根 pnpm workspace（根 `pnpm-workspace.yaml` + 根 `package.json`），**依赖统一在仓库根安装**（根 `pnpm install`），单份根 `pnpm-lock.yaml`（子工程各自 lockfile 已删除，勿再生成）；Gradle 子工程 `pnpmInstall` 的 `workingDir` 已指向 rootDir 且 `doNotTrackState`（node_modules 在根共享），`./gradlew build` 自动从根安装。
- UI 单独操作：仓库根 `pnpm install` 后，`cd ui && pnpm dev`（无需各自 install）。注意 `dev` 是 `vite build --watch --mode=development`（监听重建，不是开发服务器）。
- UI 校验：`pnpm type-check`（vue-tsc）、`pnpm test:unit`（vitest）、`./gradlew :ui:pnpmCheck`（check 任务自动跑）。`pnpm lint` = oxlint + eslint，两者均带 `--fix`，**会直接改写文件**；格式化用 `pnpm format`（prettier）。
- 悬浮名片卡片独立构建（主题组件，2026-09-11）：仓库根 `pnpm install` 后 `cd packages/float-profile-widget && pnpm build` —— **Lit + shadow DOM** 组件（lit 运行时打进产物），vite lib mode（iife + esbuild minify）产出**单文件**压缩版 `float-profile-widget.js`（样式内聚于组件、无独立 CSS），`copyToStatic` 插件自动拷入 `src/main/resources/static/widgets/float-profile-widget/`；**改 `packages/float-profile-widget/src/` 源码后必须重新构建再打包插件**。

## 关键约定与坑

- 后端 `compileOnly 'run.halo.app:api'`（BOM `run.halo.tools.platform:plugin:2.26.0`）：依赖 Halo API 编译即可，禁止打进插件。
- 根包 `cn.ialley.unihalo`；插件清单 `src/main/resources/plugin.yaml` 的 `metadata.name: uni-halo`（**2026-09-14 起由 `uni-halo-plugin` 改名，勿用旧名拼 URL**；Halo 按 metadata.name 挂插件端点，ReverseProxy 静态资源访问路径为 `/plugins/uni-halo/assets/static/{file}`，插件 ID / Console URL 亦随之变化），`spec.requires: ">=2.26.0"`，已追加 `settingName` / `configMapName` 业务字段（**这两个值刻意保留旧名 `uni-halo-plugin-settings` / `uni-halo-plugin-configmap`**：`settingName` 与 `extensions/setting.yaml` 的 `metadata.name` 强绑定，改名会让插件启动失败，`configMapName` 改名会丢配置；与插件名无关，勿顺手改）。
- **API 分组**：公开 `api.unihalo.ialley.cn/v1alpha1`，控制台 `console.api.unihalo.ialley.cn/v1alpha1`。`Constants.BASIC_DOMAIN_NAME` 是唯一源头；新增公开 GET 接口必须同步 `extensions/role-anonymous.yaml` 的 `rules`，否则 403。
- **自定义 POST 子动作路由必须用 `资源/-/动作` 三段式**（如 `love-modules/-/unlock`、`mini-program-links/-/submissions`、`auth/-/login`）：两段式 POST（`资源/动作`）会被 Halo `RequestInfoFactory` 解析为非资源请求，RBAC 资源规则失效（匿名接口 403、管理接口鉴权错乱）。三段式的资源名（`-` 前那段）才参与 role-template 的 `resources` 匹配。**所有调用方**（app 端 api、内联注入的主题模板脚本、float-profile-widget 等 widget、UI console）拼 URL 时都带 `/-/`——已有两处因写漏导致 404（`LoveDiaryHeadProcessor` 的 moduleUnlockApi、widget 的 LINK_SUBMIT_URL，2026-09-22 修复）；新增子动作接口后全局 grep 一遍确认无两段式调用残留。GET 两段式（如 `love-albums/{name}`）不受此限。
- **索引与查询 API（Halo 2.22+ 规范）**：Scheme 索引一律用 `IndexSpecs.single(name, keyType).indexFunc(...)`（keyType 实现 `Comparable`，如 `Boolean.class`），不要用已弃用的 `new IndexSpec()` / `IndexAttributeFactory`；字段查询用 `Queries`（`and/equal/contains/or/not`，`and(Condition, Condition...)` 首条件+可变参）配合 `ListOptions.builder().fieldQuery(...)`，不要用已弃用的 `QueryFactory` / `FieldSelector.of`。`ReactiveSettingFetcher` 用 `getSettingValues()` / `getSettingValue()`（Jackson 3：`tools.jackson.databind.JsonNode`，不是 `com.fasterxml.jackson`）。
- UI 业务架构参考 `plugin-vote`（`D:\HaloWorkspace\uni-halo\community\reference-projects\plugin-vote\ui`）：views 内直接使用 `@tanstack/vue-query`（`useQuery`/`useMutation`，**无 composables 目录**）、列表用 `VEntityContainer` + `VEntity` + `VEntityField`、表单用 VModal 弹窗 + SubmitButton（Ctrl/Cmd+Enter 提交）。借鉴 `plugin-links/console`（`D:\HaloWorkspace\uni-halo\community\reference-projects\plugin-links\console`）：**领域类型统一放 `ui/src/types/index.ts`**、纯函数封装在 `ui/src/utils/`、列表筛选用统一组件 `ui/src/components/common/FilterDropdown.vue`（VDropdown + VDropdownItem，点击已选项清除）。构建保持 Vite，未引入 Rsbuild。
- **列表筛选与排序统一约定（2026-09-11 起，参考 plugin-photos）**：筛选/排序一律「拆分 + 清除」——单值筛选用 `ui/src/components/common/FilterDropdown.vue`（点击已选项清除）；排序用 `ui/src/components/common/SortDropdowns.vue`（`fields: SortField[]` 把排序拆成多个维度下拉，**维度可自定义 `options`**（缺省「默认/倒序」两项；选项 value 直接对应后端 sort 参数，如 `statusFirst:PENDING`），互斥单值）；任一条件激活时显示 `ui/src/components/common/FilterCleanButton.vue` 清除按钮（`hasFilters` computed 判断 + `handleClearFilters` 重置，keyword 搜索框独立保留）。列表 useQuery 的 queryKey 已含筛选/排序状态，v-model 变化自动重新查询，无需手动 refetch。首个落地：申请审核列表（状态筛选 + 申请时间/审核时间/审核状态三个排序维度；其中**审核状态为 4 选项「默认/待审核/已通过/已拒绝」**，后端 `MiniProgramLinkSubmissionServiceImpl` 的 sort 支持 `statusFirst:{STATUS}` 按目标状态优先、**默认排序=待审核优先**）。
- `generatePluginComponentsIdx` 与配置缓存不兼容（已在 build.gradle 声明 `notCompatibleWithConfigurationCache`），不要尝试启用 configuration cache。
- `.editorconfig`：Java/Gradle 缩进 4 空格、前端 2 空格，LF 行尾；Java 与前端 max_line_length 100。UTF-8。
- `.docs/` 是规划/历史文档：迁移、app 升级设计、API 域名变更方案均已落地实施（通知公告设计见 `notice-announcement-design.md`），文档保留作为决策与执行记录；写新 API 前先确认当前域名常量（`unihalo.ialley.cn`）与 role 放行。
- `workplace/` 为空置目录，勿放代码。
- UI 入口 `ui/src/index.ts` 用 `@halo-dev/ui-shared` 的 `definePlugin` 注册路由/扩展点，`@` 别名指向 `./src`。多页面业务按子目录组织（如 `views/app-manage/`、`views/love-manage/`、`views/notice-manage/`），菜单用「父路由 + children 原生二级菜单」模式（由 Halo 菜单系统渲染，父路由组件仅含 `<RouterView />`）。**页面/组件内部 import 优先使用 `@` 别名**（`@/api`、`@/types`、`@/components/...`），不用相对路径。
- **富文本编辑器（公告正文）**：`@halo-dev/richtext-editor@2.26.0`（版本对齐 Halo 2.26，peerDependency `vue ^3.5.x`）。**不要引入其 `dist/style.css`**：bundler-kit 将 `@halo-dev/*` 视为 shared 依赖，禁止子路径导入（构建报 `Unsupported shared dependency subpath`），且该包为 Halo 宿主内置依赖，样式已由控制台全局加载。组件封装见 `ui/src/components/common/RichTextEditorField.vue`，按官方 README 用法：`new VueEditor({...})` 在 `onMounted` 中创建（`shallowRef` 持有实例），扩展用预置 `ExtensionsKit.configure({...})`（含全部官方扩展，无需手写 `Extension*` 列表），正文 HTML 用 `getHTML()` / `setContent()` 交换。
- **公告模块约定**：`Notice` 与 `NoticeType` 两个 Scheme（公告 `spec.typeName` 引用类型 `metadata.name`，删除类型不影响已关联公告）；公告字段含 `summary`（摘要，手填优先、留空服务端自动从正文剥离）、`cover`（封面图）、`link`（外链）、`publishTime`（状态为 published 时服务端自动记录，非 published 清空）；公开列表接口脱敏 `content` 且内嵌类型标签 `typeDisplayName`/`typeColor`。公告编辑弹窗采用**左右布局**（左侧基础表单 `w-80` + 右侧富文本自适应），见 `components/notice-manage/NoticeEditingModal.vue`；公告类型在**公告列表页左侧类型栏**管理（`lg:w-64`：「公告类型」标题 + 新建类型按钮（直接弹新建表单）+ 全部公告/类型列表点击筛选高亮 + **拖拽排序**（`vue-draggable-plus`，后端 `PUT /notice-types/order` 按 names 更新 priority）+ 项右侧 hover 编辑/删除图标，对齐应用管理-版本管理布局），无独立页面/二级菜单/管理弹窗；菜单为**单入口**（「公告管理」直接进入列表页，无父路由/Layout）；公告列表右侧 header 支持排序下拉（`NOTICE_SORT_OPTIONS`：日期最新在前/最早在前/按类型分组，走 `GET /notices` 的 `sort` 参数），新字段/新模型设计见 `.docs/notice-announcement-design.md`（D9–D12）。

- **审核模式模块**：`AuditDataConfig` 单例模型（`unihalo.ialley.cn/v1alpha1`，固定 name `audit-data-config`，设计见 `.docs/audit-data-model-design.md`）承载审核模式选中引用（spec 含 `posts/categories/galleryGroups/moments/linkGroups` 五个 name 列表 + `description`，不存数据副本）；管理入口为独立菜单「审核模式」（`/audit-config`，前端 `views/audit-config/` + `components/audit-config/`，UnoCSS `:uno:` 类 + Halo 组件； **页面为左右布局**（2026-09-08 起，参考功能设置）：顶部说明卡保留，下方左侧「数据分类」VCard 列表（`lg:w-64`：文章/分类/图库分组/瞬间/链接分组，条目=图标+名称+已选 N 条徽标（未配置琥珀色），点击切换选中高亮，`activeType` 状态）＋右侧 VCard 展示当前类型内容（header=图标+标题+描述+已选徽标+「选择数据」按钮，body=批量操作条（全选/删除选中）+ `vue-draggable-plus` 拖拽排序列表（已失效标记、移除按钮）+ 空状态 VEmpty，见 `AuditConfigView.vue`））；管理端接口 `GET/PUT plugins/uni-halo/audit-data`（GET 返回 `AuditDataConfigDetail`=原始配置+各类型已选详情，已删引用由前端标记「已失效」）、`GET .../audit-data/candidates?type=post|galleryGroup|moment|linkGroup`（选择器数据源； **type=category 已改为前端直调 `@halo-dev/api-client` 的 `consoleApiClient.content.category.listCategoryTree()`**（2026-09-08，自动处理认证，spec 含 cover/priority 完整字段，本地平铺 + 关键字过滤 + 分页映射 AuditDataRef，见 `ui/src/api/index.ts` auditDataApi.candidates））；公开只读接口 `GET api.unihalo.ialley.cn/.../audit-data`（联动功能设置-应用设置-审核模式 `auditMode.enabled`（2026-09-11 起由设置页 `safetyConfig.auditConfig` 迁入），返回剔除失效后的 spec，供小程序端二期接入）；角色模板 `role-template-audit-data.yaml`（view/manage，UI 权限 `plugin:uni-halo:audit-data:*`）。 **跨插件候选查询**：Post/Category 与 plugin-photos (PhotoGroup)/plugin-moments (Moment)/plugin-links (LinkGroup) 均通过本地同 GVK 引用类（`scheme/*Ref.java`，勿在 UniHaloPlugin 注册）+ `client.listAll` 全量内存过滤分页；分类候选/已选含封面/排序/文章数（`CategoryRef.spec.cover/priority` + `status.postCount`（缺失默认 0）→ 快照 cover/priority/postCount，2026-09-08 修复，审核模式与精选分类共用；app 端审核模式下分类页可免请求复用快照）；服务端内部 client 调用不经过 HTTP RBAC，角色模板无需声明跨插件 list 权限；公开放行由 `role-template-anonymous.yaml` 的 `api.unihalo.ialley.cn` 全资源规则覆盖。

- **插件内置静态资源（2026-09-02）**：插件默认图可打包进 `src/main/resources/static/`，经 `extensions/uni-halo-reverse-proxy.yaml`（`ReverseProxy`：`path /static/**` → `directory: static`）代理，访问路径 `/plugins/uni-halo/assets/static/{file}`（匿名可访问、不经 RBAC）；在 FeatureConfig 等自定义模型与 setting 表单字段中直接填该 **相对路径**即可（attachment 控件仅能选 Halo 附件、需手动粘贴；text 控件更顺滑）。现内置 `logo.png`（应用图标）/`uni_halo_profile_bg.jpeg`（关于页背景）/`uni_halo_about_wave.gif`（关于页与恋爱波浪）/`uni_halo_img_lazyload.gif`（加载占位）/主题组件 `widgets/float-profile-widget/`（单文件 float-profile-widget.js，2026-09-22 由 floating-window/float-mini-profile.js 迁移更名，主题端注入）等素材，同名替换即生效； **默认值为插件内置路径**（appInfo.logo、aboutConfig.bg/wave、love.diaryPage.bgImageUrl、assets.loadingGifUrl；loveConfig 波浪/爱心图已下线），外链 `uni-halo.ialley.cn` 默认图依赖已清除；`assets`（imagesConfig）2026-09-08 起仅保留 loading/error 两键（默认图片/空图片配置已下线），由站长配置或客户端内置回退。

- **功能设置改名与迁移（2026-09-15）**：原「通用配置」整体更名「功能设置」——Java 类 `FeatureConfig`（GVK kind 同步改为 `FeatureConfig`/`featureConfigs`，单例名 `feature-config`）、console API `plugins/uni-halo/feature-config`、角色模板 `role-template-feature-config.yaml`（权限 `plugin:uni-halo:feature-config:*`）、UI `views/feature-config/` + 路由 `/feature-config`。旧通用配置迁移逻辑（`LegacyGeneralConfig` + `migrateFromLegacy()`）已于 commit fb5c5a7 彻底删除。**恋爱日记页配置迁入 `spec.love.diaryPage`**（`pages.loveDiaryConfig` 字段已直接删除不保留 @Deprecated）；app 端输出 shape 不变（仍 `pageConfig.loveDiaryConfig`，数据源换 `spec.love.diaryPage`）、模板 Finder 改读新位置，均无感。UI：恋爱设置新增「页面设置」tab（页面设置/恋爱信息/页面入口/模块入口），页面设置 tab 删「恋爱日记」；「友链信息」改名「友链设置」、「基本配置」改名「基本设置」（数据键 linkInfo 不变）。
- **spec.pages 键名统一（2026-09-21）**：`FeatureConfig.Pages` 下存储键/Java 字段统一为裸语义名（无 Page/Config 后缀），Java 内部类统一 `XxxPage`；**不做存量数据迁移**（新插件新 app，旧键数据自然失效）：`homeConfig→home`（类 `Home`→`HomePage`）、`momentPageConfig→moment`、`aboutConfig→blogger`（博主页，原「关于页」遗留命名）、`myPageConfig→mine`（类 `MyPage`→`MinePage`）、`disclaimers→disclaimer`（单数）、`postDetailConfig→postDetail`（类 `PostDetail`→`PostDetailPage`）；`titles` 与 `agreement` 键不动（`titles.disclaimers` 同步单数化为 `titles.disclaimer`）。规则：**JSON 键/字段=裸语义名、类名=语义名+Page**；`pages.titles.xxx` 与 `pages.xxx` 同词对应同页面。app 端消费 `featureConfig.pages.{home,blogger,mine,moment,postDetail,disclaimer}.*` 需同步发版。
- **功能设置模块（FeatureConfig，2026-09-02 P1a 已实施）**：`FeatureConfig` 单例模型（`unihalo.ialley.cn/v1alpha1`，固定 name `feature-config`，设计见 `.docs/config-system-v2-redesign.md` v2.3 与 `.docs/site-display-config-uiux-design.md`；2026-09-10 内容重组见 `.docs/feature-config-content-reorg-design.md`）承载原 setting.yaml 迁出的「小程序通用内容与外观」：spec 分 `profile`（应用资料：应用信息 name/logo + 博主（含主页 website，2026-09-10 起原「官网地址」改名；**介绍 intro** 富文本 HTML，app 端联系博主页 mp-html 渲染）/社交（**2026-09-10 起动态列表 items**：**2026-09-11 起去掉 key 平台标识**（图标由 app 端按 color/bgColor 色块渲染），name/content/color/bgColor/priority/visible，去 enabled 开关，控制台逐项编辑拖拽排序，app 端联系博主页按序渲染/点击复制，未配置即空态不回退默认；旧固定字段由 legacyOverlay 迁移）/审核模式 auditMode（2026-09-11 由设置页 safetyConfig.auditConfig 迁入，enabled 开关，经 getConfigs 重建回旧 auditConfig.auditModeEnabled 形态下发）/页脚版权 copyrightConfig（2026-09-10 由页面设置-关于页迁回，显示于【关于】页面页脚）；免责/文章详情 2026-09-10 起迁入 pages；`showAboutSystem` 下线由功能入口注册表管理）、`pages`（首页/图库页/分类页/瞬间页/关于页；startConfig 已下线不在此；2026-09-10 起：新增文章详情页 postDetailConfig、新增免责声明页 disclaimers（去启用开关仅 content），页脚版权 2026-09-10 迁回应用资料 profile.copyrightConfig（不再在 aboutConfig 内）；2026-09-08 起：首页标题与轮播渲染参数不再配置（轮播 app 端默认开启，`bannerConfig` 字段删除），首页新增快捷导航逐项配置 `quickNavigation`（每项 key/title/subTitle(对标app端rightText，可空)/color/bgColor(原bgGlass)/iconPrefix/icon/path/visible(原show)，**默认色统一 hex8（#rrggbbaa，2026-09-11 起与 FormKit `type="color"` `format="hex8"` 输出一致，弃用 rgba()）**，默认 5 项对齐客户端 `uh-home-quick-nav`，排序=数组顺序；**app 端 `uh-home-quick-nav` 直接读 `quickNavigation` 渲染（2026-09-08 起字段命名即插件端：bgColor/visible，无 bgGlass/borderColor/show 映射，未配置回退内置默认项、同字段命名）**；控制台 UI 为一行紧凑布局：FormKit 输入框（**label 置空、用 CSS 自定义左侧 label 布局**（flex 容器内 span 文本 + 输入框）；**2026-09-11 起去掉标识 key 列，仅维护名称/文字颜色/图标背景色/显隐**）+ 文字颜色与图标背景色用 **FormKit `type="color"`**（`format="hex8"` 输出 #rrggbbaa 含透明度，`toColorInput` 透传回显）+ 显示开关；**2026-09-10 起放开增删**：候选弹窗 `FeatureEntryCandidatesModal`（前端静态注册表 `FEATURE_ENTRY_REGISTRY`，**2026-09-10 起统一清单展示全部注册表条目、不按 group 分组**、已配置置灰禁选）添加 + 删除 + 拖拽排序 + 「恢复默认」（=注册表 home 组默认配置，排序/visible 一并恢复））+ 首页分类栏选中引用 `categories`（固定 3 个，**快照 name+displayName+cover+priority+postCount**（2026-09-08 起含封面/排序权重/文章数：priority 取 Halo Category.spec.priority、postCount 取 status.postCount 缺失默认 0），数组顺序=展示排序；选择复用审核模式候选弹窗 `AuditCandidatesModal` type=category max=3；管理端展示为类审核模式列表（缩略图+名称+拖拽排序+移除）；保存后 app 端直接按快照渲染、**不再请求分类接口**（未配置时回退请求））；图库页瀑布流配置下线；分类页/瞬间页新增标题 `pageTitle`（输出 pageConfig.categoryConfig/momentConfig，客户端现有 type/useTagRandomColor 保留）；**2026-09-11 起新增 10 个功能页面标题配置**（contactConfig 联系博主 / favoritesConfig 我的收藏 / friendLinksConfig 友情链接 / archivesConfig 文章归档 / voteConfig 投票中心 / dataVisualConfig 数据看板 / settingConfig 偏好设置 / aboutProjectConfig 关于项目 / noticeConfig 公告中心 / searchConfig 搜索页面，均仅 pageTitle、默认留空客户端回退内置标题，经 getConfigs 输出 pageConfig.xxxConfig；免责声明页 disclaimersPage tab 已存在未重复添加）；**2026-09-10 起新增我的页面功能入口 `myPageConfig`**（常用功能/其他功能两组，配置并入「关于页」tab，经 getConfigs 下发 `pageConfig.myPageConfig`（additive、两组全空不输出），条目复用快捷导航项结构（含 subTitle；subTitle 对齐 app 端 about.vue 本地默认 rightText，控制台「提示」输入框逐项编辑，2026-09-11 起去标识 key 列），候选弹窗 `FeatureEntryCandidatesModal` 添加 + 删除 + 拖拽排序 + 「恢复默认」（=注册表对应组默认配置）；**默认填充注册表条目**（2026-09-11 起常用 7 项（2026-09-16 起新增通知公告为 8 项）/ 其他 3 项，对齐 app 端 about.vue navList，前端 `defaultConfig` 与后端 `FeatureConfigServiceImpl` 默认一致）；设计见 `.docs/feature-entry-unified-design.md`））、`assets`（加载占位 loadingGifUrl/loadingErrUrl；2026-09-08 起默认图片/空图片配置已下线，客户端内置回退）、`preferences`（站点展示偏好 L0 默认，2026-09-08 起按页面分组，字段与客户端 `hermes/preferences.md` §5 映射对齐：homeListLayout/homeCardType/articlesListLayout/articleCardType/archivesListLayout/archivesCardType/avatarRadius——列表布局值 `single`/`double`、卡片样式值 `image_top`/`image_right`/`image_bottom`/`image_left`（旧 `lr_*`/`tb_*`/`h_row_col1/2` 值体系废弃），getConfigs 顶层新增 `preferences` 下发，**2026-09-08 起客户端去映射：数据结构直接采用插件字段名（顶层 homeListLayout/homeCardType/articlesListLayout/articleCardType/archivesListLayout/archivesCardType/avatarRadius），collectSiteDefaults 退化为值校验透传，消费组件直读新字段**；avatarRadius 评论头像 2026-09-08 起控制台 UI 暂时隐藏，后端字段与下发保留）、`love`（恋爱模块：恋爱日记入口 `loveDiary`（恋爱页本身入口密码，2026-09-12 起由「模块入口」迁至「页面入口」tab、**无 enabled 开关**，入口显隐由页面设置-快捷导航/关于页功能入口注册表控制，设置密码后进入恋爱页前需先验证（恋爱配置已统一经 getConfigs 下发，无独立 /love-config 接口））+ 恋爱信息 `loveInfo`（纪念日+恋人信息，管理端配置在恋爱设置-恋爱信息 tab，经 getConfigs 下发 `loveConfig.loveInfo`（独立 `/love-config` 接口已于 2026-09-12 下线））+ 三模块入口 ourStory/lovePhoto/loveDaily（恋爱故事/相册/清单，配置在恋爱设置-模块入口 tab；**2026-09-12 起模块入口自身即 app 端入口列表数据**：title 名称/subTitle 副标题/titleColor/subTitleColor 颜色（hex8）/iconBgColor 图标背景色/path 跳转路径/priority 排序（拖拽排序、越大越靠前）+ enabled 开关 + 入口密码；默认值对齐 app 端 love.vue 硬编码：故事/清单 titleColor #f83856、subTitleColor #f8385699、iconBgColor #fce7f3，相册 #60a5fa/#93c5fd/#dbeafe，path 为 pages-blog/love/ 下 stories/album/list；**navList 已删除**（stories/album/list 展示 key 不再下发，app 端删除 DEFAULT*NAV_LIST/MODULE_KEY_MAP 统一用模块 key，locked=passwordEnabled 且本地无有效 token）），恋爱页背景图由恋爱设置-页面设置 `spec.love.diaryPage.bgImageUrl` 承担、`linkInfo`（友链信息，2026-09-08 起拆分子结构（2026-09-10 起去作者信息与站点联系邮箱；2026-09-11 起新增基本配置）：**submissionEnabled**（基本配置：是否开放公开提交申请，原 setting 基本设置-友情链接，2026-09-11 迁入，默认 true，关闭后公开提交接口返回「暂未开放提交申请」且 app 端隐藏提交入口）、**miniInfo 小程序信息**（displayName/miniProgramCode/link/description/applyRemark，app 端「申请信息」弹窗展示）、**siteInfo 站点信息**（本站站点名片，字段对齐 Halo 官方 plugin-links 友链提交 API `link-applications` 请求体：displayName/url/logo/description/backlink/feedUrls，不再维护 email）、**authorInfo 作者信息已下线**（改由应用设置-博主资料承担：app 端 uh-links-mini-info 改读 authorConfig.blogger 的 nickname/avatar/website，官网地址 website 2026-09-10 新增）；经 getConfigs **直接下发 `pluginConfig.linkInfo`（字段名无映射）**；**linksSubmitPlugin 已弃用**（setting.yaml 的 linksSubmitPlugin 组已删除、不再下发；app 端 uh-links-mini-info 读 miniInfo+authorInfo、uh-links-site-info 读 siteInfo、第三方 LinksSubmit 提交功能暂未开放），见 `PublicConfigAssembler` linkInfo 段）。管理端接口 `GET/PUT plugins/uni-halo/feature-config`（console；GET 不存在时返回「默认值 + 历史 ConfigMap 旧值合并」，首次保存做非空合并导入，见 `FeatureConfigServiceImpl`）；角色模板 `role-template-feature-config.yaml`（view/manage，UI 权限 `plugin:uni-halo:feature-config:*`）；Scheme 注册于 UniHaloPlugin（单例无索引）。**getConfigs 输出由 `utils/PublicConfigAssembler` 合成**：authorConfig/pageConfig/basicConfig(内容部分)/imagesConfig/loveConfig（← spec.love）从 FeatureConfig 重建，并剔除 tokenConfig/startConfig/auditModeData，其余组（appConfig/captchaConfig/pluginConfig/linkConfig）透传、auditConfig 由 spec.auditMode 重建（2026-09-11 起，输出 shape 不变）——输出 shape 冻结，小程序端无感。setting.yaml 已按**方案 B 三域重组**（featureConfig 功能模块=linkConfig，loveConfig 已 2026-09-03 整体迁出；safetyConfig 安全与风控=auditConfig/captchaConfig；integrationConfig 平台接入=appConfig/pluginConfig，模块内容作为域组下 `$formkit: group` 子组；**2026-09-10 起 `pluginConfig` 删除 `votePlugin`/`linksPlugin` 两子组（app 端已改用「检测插件是否启用」判定友链站点数据/投票中心，不再消费这两个开关），`toolsPlugin` 保留；`PublicConfigAssembler` 输出端对 `pluginConfig` 剔除旧 ConfigMap 残留的 `votePlugin`/`linksPlugin` 键，防历史配置继续透传**）；服务端读取一律经 `utils/SettingGroupResolver.group(fetcher, 域, 模块)`（域路径优先、旧顶层键回退），公开输出由 `PublicConfigAssembler` 把域组展开还原为旧模块顶层键（客户端 shape 不变）；`loveDate/loveInfo` 等已模型化字段的历史 ConfigMap 残留随 2026-09-03 输出重建（loveConfig ← spec.love typed）**不再透传**（原偏差记录关闭，客户端 love.vue 降级分支随 P3 清理）。setting 现为 4 个 Tab：基本设置（2026-09-11 由「功能模块」改名，仅剩友情链接-审核邮件相关） + 安全控制（2026-09-11 由「安全与风控」改名；审核模式 auditConfig 组 2026-09-11 迁出至功能设置-应用设置，仅剩防刷验证码 captchaConfig） + 平台接入（仅 pluginConfig） + 主题展示（2026-09-10 新增：主题悬浮窗 floatingWindow，见下方「主题悬浮窗模块」）——「基本配置」Tab 已移除（应用信息并入功能设置「应用资料」，历史 ConfigMap 的 baseConfig/appConfig 旧值由 FeatureConfig 导入兜底）；**动态小程序码/海报配置字段（useDynamicQRCode/appId/appSecret/policyName/fileGroupName/qrCodeImageUrl）已下线**（海报后端流程字段缺失静默降级，模块清理待定）；**恋爱 loveConfig 已全部迁出 setting（2026-09-03）**：内容字段（loveDateTitle/loveDate/loveInfo/ourStory.content/lovePhoto.groupName/loveDaily.list）此前按 love 文档 §7.4 移除，剩余 pageImages/模块开关（enabled/iconUrl）整体迁入功能设置 `spec.love`（**2026-09-10 起总开关 loveEnabled 下线**，入口展示由模块入口开关与 navList 统一管理；前端功能设置页「恋爱」分区维护，恋爱明细数据仍在「恋爱管理」菜单）；2026-09-08 起模块开关**去掉 iconUrl、三入口（ourStory/lovePhoto/loveDaily）可分别设置入口密码**（`ModuleSwitch`：enabled + passwordEnabled + password + passwordHash + passwordRemoved，BCrypt 存储，与相册密码同一套语义；控制台 GET/PUT 响应一律脱敏哈希并派生 `passwordEnabled`，`save` 语义=新密码重设/`passwordRemoved` 清除/留空保持，`utils/LoveModuleTokenManager` HMAC 无状态 token 30 分钟）；**公开解锁接口 `POST api.unihalo.ialley.cn/.../love-modules/unlock` `{module,password}`**（module=loveDiary/ourStory/lovePhoto/loveDaily，校验通过签发 token，失败 400「密码不正确」；**2026-09-11 起接入验证码 scope=loveModuleUnlock，失败 403 + 附新验证码**），设置密码的模块其数据接口（`/love-stories`、`/love-albums`、`/love-albums/{name}`、`/love-albums/{name}/unlock`、`/love-daily-items`）要求携带 `?token=`，未带/无效返回 401 `{reason:"locked"}`（未设密码的模块不校验，老客户端无感；相册级密码保持现状=外层模块锁+内层相册锁）；**客户端已接入（2026-09-08）：love.vue 入口拦截（passwordEnabled 且无 token 弹密码框）→ unlock 签发 token 存 `utils/loveModuleToken` → love 数据接口（stories/albums/daily）自动携带 `?token=` → 401 locked 清除 token 并提示回入口**；getConfigs 的 loveConfig 输出脱敏：`loveDiary` 仅 `passwordEnabled`（无开关）；三模块输出 `enabled`+`passwordEnabled`+`title/subTitle/titleColor/subTitleColor/iconBgColor/path/priority`（按 priority 降序，`PublicConfigAssembler.sanitizeLove`），密码字段一律不下发。setting 中 loveConfig 组已删除，公开配置（getConfigs loveConfig 组）统一改读 `spec.love`（`PublicConfigAssembler`）；独立 `GET /love-config` 接口已于 2026-09-12 下线（`LovePublicEndpoint` 不再提供，客户端统一消费 getConfigs），客户端无感。前端「功能设置」页已实施：`views/feature-config/FeatureConfigView.vue`——**2026-09-10 起组件化拆分**：按左侧 7 个大分区拆为 `views/feature-config/sections/` 下 7 个独立组件（ProfileSection/PreferencesSection/PagesSection/AssetsSection/LoveSection/LinkInfoSection/MaintenanceSection），父组件仅保留左侧菜单骨架 + 右侧 `VTabbar` 子切换 + 数据加载/保存/dirty 追踪，并经 `views/feature-config/form-context.ts` 的 `FeatureConfigFormKey` **provide/inject 共享 `formState` 与 `save`**（子组件直接改 formState 嵌套属性 → 父级 `deep watch(formState)` 自动置 dirty，保存按钮状态天然同步，无需事件上报）；页面为**左右布局，左右两块卡片区均为 VCard**（左栏 `:body-class="!p-0"` + 自定义「配置分类」头行 + `px-4 py-3` 条目高亮，参考公告管理左侧类型栏；右侧内容 VCard，参考公告管理），左侧切换大分区、右侧 **Halo `VTabbar` 子切换**（应用资料：应用信息/博主（含主页 website 与介绍 intro 富文本，2026-09-10 起）/社交（2026-09-10 起动态列表，去 enabled 开关：逐项名称/内容/颜色/背景色/显示 + 拖拽排序 + 添加空行 + 删除，2026-09-11 起去标识 key，图标由 app 端按颜色/背景色色块渲染）/审核模式（2026-09-11 迁入：原设置页 safetyConfig.auditConfig 开关，VSwitch）/页脚版权（2026-09-10 迁回，enabled + content）；偏好设置：首页/文章页面/归档页面（2026-09-08 起三个子 tab，每页列表布局 single/double + 卡片样式 image*\*；评论头像仍暂时隐藏）；页面与排版：首页（快捷导航逐项配置+分类栏选择固定3个）/图库页/分类页/瞬间页/关于页（资料卡背景图与波浪图同行布局 2026-09-11；页脚版权 2026-09-10 已迁回应用设置）/文章详情页（2026-09-10 新增，评论+版权文案）/免责声明页（2026-09-10 新增，富文本无开关）；资源与兜底：加载占位（默认图 2026-09-08 起下线，仅 loading/error 两图）；恋爱：恋爱信息（纪念日+恋人信息）/页面入口（2026-09-12 新增：恋爱日记密码，无开关）/模块入口（三模块开关+入口列表数据+入口密码+拖拽排序，无图标）；友链信息：基本配置（2026-09-11 新增：开放公开提交申请开关，原 setting 基本设置-友情链接）/站点信息/小程序信息（2026-09-10 起去作者信息）），布尔项用 Halo `VSwitch`，所有表单组件单列独占一行且**不随开关显隐（常显）**，免责声明用 Halo 富文本 `RichTextEditorField`（无预览卡），页标题 `UniHalo-功能设置`；菜单「功能设置」priority=0 且注册于「欢迎使用」之后（2026-09-08 调整，位于欢迎使用下方、应用管理之前，见 `ui/src/index.ts`）。
- **维护模式配置（Maintenance，2026-09-04 并入 FeatureConfig）**：`FeatureConfig.spec.maintenance`
  （enabled / title / description / startTime / endTime；title 默认「站点维护中」，description 为富文本 HTML，
  时间统一 RFC3339 UTC 字符串）。**状态判定无状态化**：`utils/MaintenanceResolver.resolve(...)` 纯函数按
  enabled + startTime/endTime + 当前时刻计算（scheduled 预告 / active 维护中 / NONE=未维护或已到点自动结束；
  无定时任务、不物理清库，「到点自动关闭」= 输出端判定 NONE，配置不回写）；公开 `getConfigs` 顶层新增
  additive `maintenance` 键（仅 scheduled/active 输出 `{status,title,description,startTime,endTime}`，
  logo 复用 `appConfig.appInfo.logo`，键缺失即未维护，老客户端无感），**服务端不拦截接口**（小程序端本地
  路由拦截）；管理端「功能设置」页新增「维护」左侧大分区（2026-09-08 起右侧**双子 Tab「维护时间」/「维护内容」**：时间 tab=开始/预计恢复时间（原生 `datetime-local` 输入，本地时区 ↔ RFC3339 UTC 互转）、内容 tab=标题/说明(notice 纯文本)/详情(description 富文本)；四态状态卡（VStatusDot + 每秒走秒倒计时 + active 时「提前结束维护」按钮）与启用开关在 Tab 外通用）；保存预检与后端均校验 startTime ≥ endTime 拒绝
  （400）；Scheme/角色模板零新增（沿用 feature-config 的 view/manage）。设计见 `.docs/maintenance-config-design.md`。

- **验证码模块（2026-09-11 由「防刷验证码」改名）**：`captcha/` 包（`CaptchaType` ALPHANUMERIC/ARITHMETIC + `CaptchaGenerator` AWT 生成 + `CaptchaManager` 内存缓存：**1 分钟过期、上限 100、校验后作废一次性**，时钟可注入测试 + `CaptchaService` 读设置开关 + `CaptchaEndpoint` 公开 `GET plugins/uni-halo/captcha/generate` 返回 `{id, imageBase64}`）。受保护公开写接口：`POST /mini-program-links/submissions`、`POST /love-albums/{name}/unlock`、`POST /love-modules/unlock`（2026-09-11 接入，覆盖恋爱日记/恋爱故事/恋爱相册入口/恋爱清单等模块入口解锁），验证码通过 **query 参数** `captchaId/captchaCode` 传递、endpoint 内 `requireValid` 校验，失败返回 **403 + 附新验证码**（前端即时刷新重试）；开关在设置页「安全控制」`captchaConfig` group（label 已由「防刷验证码」改名「验证码」；enabled 默认开 / **scope 生效范围：linkSubmission、loveAlbumUnlock、loveModuleUnlock（2026-09-11 新增），默认全开，接口侧按范围生效、scope 缺省视为开启** / type / captchaLength / arithmeticRange）。设计见 `.docs/captcha-anti-spam-design.md` 与 `.docs/captcha-scope-design.md`。

- **主题组件模块（2026-09-22 由「主题悬浮窗」通用化，原设计见 `.docs/floating-window-design.md`）**：向主题页面注入主题组件（首个为悬浮名片卡片，原小程序太阳码悬浮卡片）。**机制（通用注入器）**：`process/ThemeWidgetHeadProcessor`（`@Component` 实现 `TemplateHeadProcessor`，经 `extensions/theme-widget-head-processor.yaml` 声明 ExtensionDefinition `theme-widget-head-extension`）内置 **widget 注册表**（`WidgetSpec`：configKey/globalKey/jsPath 三元组，新增 widget 只需注册表加一条 + setting.yaml `themeWidgetConfig` 组加子组 + packages 下新增前端包，Java 零改动），逐 widget 在每页 `<head>` 注入内联配置 + 静态脚本（defer，URL `/plugins/uni-halo/assets/static/{jsPath}?version={插件版本}`，经 `extensions/uni-halo-reverse-proxy.yaml` 暴露，带版本号防缓存；**每个 widget 独立 fail closed**：未启用/配置异常只跳过该 widget 不影响其他）。**悬浮名片卡片（floatProfileWidget）**：全局键 `window.__UNI_HALO_FLOAT_PROFILE_WIDGET__`，脚本 `static/widgets/float-profile-widget/float-profile-widget.js`（由 `packages/float-profile-widget` vite 独立工程构建产出，**Lit + shadow DOM** 自定义元素 `<uh-float-profile-widget>`，样式完全内聚隔离，CSS 类前缀 `uh-fpw`），imageUrl 为空不注入；**localStorage 一次性迁移**：旧键 `uh-fmp-closed`（sessionStorage 关闭记忆）/`uh-fmp-apply-draft`（localStorage 申请草稿）→ 新键 `uh-fpw-*`（config.ts `migrateLegacyKeys`，新键为空搬移后删旧键）；**配置存放 setting.yaml `themeWidgetConfig` 域（Tab「主题组件」）`floatProfileWidget` 子组**，服务端 `SettingGroupResolver.group(fetcher, "themeWidgetConfig", "floatProfileWidget")` 读取；**配置下发走服务端内联注入**（无新增公开接口、不动 role-anonymous.yaml；CSP 严格主题可能拦截内联脚本，备选「js fetch 公开接口」未实施）。**setting 域重组（2026-09-22）**：原 `themeConfig`（Tab「主题展示」）拆分——新增 `themeWidgetConfig`（Tab「主题组件」，widget 配置，`PublicConfigAssembler` 白名单已同步透传），原组更名 `themeTemplateConfig`（Tab「主题模板」，仅剩 `loveDiaryTheme` 恋爱日记主题页，`Constants.SETTING_DOMAIN_THEME_CONFIG` 与 `LoveDiaryHeadProcessor`/`LoveDiaryConfigResolver` 已同步）；**键改名后旧悬浮窗配置丢失一次，站长需在「主题组件」重新保存**。**展示形态**：太阳码（imageUrl）+ 名称（name/nameSize/nameColor）+ 描述（description/descSize/descColor），右上角 × 关闭（closeEnabled，rememberClosed 时 sessionStorage 记忆 `uh-fpw-closed`），文本一律 textContent 渲染防注入。**申请弹窗增强（2026-09-11）**：**顶部分段器拆分两面板**（基础信息：小程序名称/太阳码/地址/分组/描述/申请说明/预览图；作者信息：昵称/网站/邮箱），底部共享操作区（验证码 + 重置/取消/提交，固定不滚动）；预览图**动态添加行**（URL 输入 + 删除 + 「添加一张」）；表单输入自动缓存 localStorage（key `uh-fpw-apply-draft`，刷新/关闭后重新打开回填，验证码不缓存；提交成功或点「重置」清空），表单 `novalidate` + 提交时手动校验必填；提交按钮用 app 主题色（`#37c2bc`）+ 黑色文字；信息弹窗与申请表单弹窗均为 glass 设计。**交互**（Lit 组件 + shadow DOM，工程见 `packages/float-profile-widget`）：9 向锚点（position：8 向 + 屏幕居中 center，offsetX/offsetY 偏移走 inline transform）、拖拽（dragEnabled，Pointer Events 兼容触屏，拖后自由定位 left/top）、默认状态（defaultState=default 卡片 / minimized 小球；**2026-09-24 简化：去掉贴边隐藏（edgeHideEnabled/edgeHideDistance）及贴边态记忆，会话级状态记忆 `uh-fpw-state` 保留但 WidgetPersistedState 去掉 edge 字段（仅 minimized/cardPos/dotPos + 配置指纹）**）、页面显示范围（pageScope=all/only/except + pagePatterns 每行一模式，`*` 非 `/`、`**` 任意、空或 `/` = 仅首页，对齐 plugin-announcement matchUrlPattern）。插件停用/卸载后注入自动消失。
- **轮播管理模块**：`Banner` 归一化条目 Scheme（`unihalo.ialley.cn/v1alpha1`，plural banners，设计见 `.docs/banner-design.md`）。 **归一化要点**：所有条目同一套 spec（title/cover/date/authorName/authorAvatar/content/remark/postId/link/priority），`source`（post/custom）由服务端按 `postId` 非空自动判定并强制覆盖， **前端不提交 source**；文章模式仅快照 title/cover/date/authorName/authorAvatar/postId、content 恒为空，自定义模式 content 为富文本 HTML。 **作者信息**：文章模式快照 `Post.spec.owner`（Halo 2.x 的 `Metadata`/`MetadataOperator` 均无 owner 字段，作者在 Post spec 中）并查 `security.halo.run` User 扩展取 displayName/avatar、User 缺失回退 owner 用户名； **自定义模式作者默认取当前登录用户**（BannerEndpoint 内 `request.principal()` → User 填充，前端不显示/编辑作者字段）。候选查询复用 `PostRef`（含 publish/owner 字段）+ `client.listAll` 全量内存过滤（仅已发布），管理端接口 `GET /banners/candidates`。公开接口：`GET api.unihalo.ialley.cn/.../banners` 列表 **脱敏**（不含 content/remark）、`GET .../banners/{name}` 详情含 content；`/order`、`/candidates`、`/{name}/sync` 必须注册在 `/{name}` 之前（避免被当作 name 匹配）。设置页 `bannerConfig` 仅保留渲染参数（enabled/showTitle/showIndicator/height/dotPosition，type/list 已移除），数据在独立菜单「轮播管理」`/banners`（单入口，`views/banner-manage/` + `components/banner-manage/`）维护：列表 **多选批量操作**（批量删除 / 批量同步快照，仅文章条目 `POST /banners/{name}/sync` 重新拉取 Post/User 覆盖）+ **来源筛选/排序**（`GET /banners` 的 `source=post|custom`、`sort=manual|date_desc|date_asc`，非手动排序不渲染拖拽手柄）+ 拖拽排序（`vue-draggable-plus` → `PUT /banners/order` 按 names 更新 priority）+ **双弹窗拆分**：新建（从文章）多选候选批量创建（仅提交 postId，`BannerCandidatesModal` 用 Halo `SearchInput` 全局组件，`multiSelect` prop 区分多选/更换文章单选）、新建（自定义）/自定义条目编辑用纯自定义弹窗（无来源切换，`RichTextEditorField` 富文本，label「日期」）； **文章条目不支持编辑内容**（行操作=同步快照/更换文章/删除，自定义条目=编辑/删除）；角色模板 `role-template-banner.yaml`（view/manage，UI 权限 `plugin:uni-halo:banner:*`），公开放行由 `role-template-anonymous.yaml` 全资源规则覆盖。

- **删除语义统一（跨模块约定）**：所有可删业务 Scheme 统一采用 finalizer + `deletionTimestamp` 异步删除（设计见 `.docs/deletion-finalizer-design.md`，Banner 试点已落地，其余按阶段 2 铺开）。
    - **后端**：新增 Scheme 默认继承 `reconciler/AbstractFinalizerReconciler<T>`（finalizer 常量 `unihalo.ialley.cn/finalizer`），子类 ~10 行（见 `reconciler/BannerFinalizerReconciler.java` 样板）；删除流程 = Service.delete 业务校验（保留，失败 400 不产生标记）→ `client.delete` 打 `deletionTimestamp` → Reconciler 按 `deletionDelay()`（默认 1s，可覆写 `Duration.ZERO`）requeue 过渡 → `cleanUp()` 钩子（默认空）→ `removeFinalizers` → 物理删除。**例外不纳入**：`AppVersion`（业务软删除 `isDeleted`）、单实例 `AuditDataConfig`/`FeatureConfig`、无管理列表的 `QRCodeInfo`。
    - **读路径过滤**：公开 `api.unihalo.ialley.cn` 接口/候选查询/模板 finder 必须排除删除中对象（`isNull("metadata.deletionTimestamp")` 或详情按已删除视为不存在）；**管理端列表不过滤**（靠保留删除中项支撑前端「删除中」徽标与轮询）。勿给管理端与公开端共用的 list 方法直接加过滤（如 Notice 公开列表复用 `noticeService.list`），需拆分/加参。
    - **前端接入**：列表 `useQuery` 加 `refetchInterval: (data) => deletingRefetchInterval(data)`（`ui/src/utils/query.ts`，**勿直传函数引用/写成泛型**，否则污染 data 类型推断）；删除操作统一走 `useDeletionFlow(queryKey).confirmDelete(...)`（`ui/src/composables/useDeletionFlow.ts`，收口 Dialog/Toast/invalidate）；列表项在 `item.metadata.deletionTimestamp` 存在时渲染「删除中」`VStatusDot`（对齐 `BannerListView.vue`）。

## 维护规则

当项目结构、构建/测试命令、架构边界、开发约定或本文档记录的其他事实发生变化时，须在同一改动中同步更新本文件。
